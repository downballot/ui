# ui
Downballot UI

# Setup
```
# Install and use the LTS version of Node.
nvm install --lts
nvm use --lts

# Enable corepack (yarn).
corepack enable

# Make sure that you're using the correct version of yarn.
yarn set version stable
```